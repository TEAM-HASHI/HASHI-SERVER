apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus   # 대시보드 JSON이 이 uid로 참조한다 — 변경 시 대시보드도 함께 수정
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

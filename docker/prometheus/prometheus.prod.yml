global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: hashi-server
    metrics_path: /actuator/prometheus
    static_configs:
      # prod compose의 app 서비스 — 같은 도커 네트워크 내부 통신이라 호스트 포트 개방 불필요
      - targets: ['app:8080']
